# scrollToTopJS

A simple animate scroll to top Plugin

## Example

```code
Default

$(Selector).scrollToTop();

Setting options
* distance from top
* animation speed in milliseconds

$(Selector).scrollToTop({
    top: 100,
    speed: 1000
});
```